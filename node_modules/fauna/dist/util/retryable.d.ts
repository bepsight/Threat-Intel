export type RetryOptions = {
    maxAttempts: number;
    maxBackoff: number;
    shouldRetry?: (error: any) => boolean;
    attempt?: number;
    sleepFn?: (callback: (args: void) => void, ms?: number) => void;
};
export declare const withRetries: <T>(fn: () => Promise<T>, { maxAttempts, maxBackoff, shouldRetry, attempt, sleepFn, }: RetryOptions) => Promise<T>;
